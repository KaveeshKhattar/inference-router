apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "inference-router.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: inference-router
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
