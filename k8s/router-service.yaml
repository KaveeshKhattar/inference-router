apiVersion: v1
kind: Service
metadata:
  name: inference-router
spec:
  selector:
    app: inference-router
  ports:
    - name: proxy
      port: 80
      targetPort: 8080      # or whatever your router listens on
    - name: metrics
      port: 9000
      targetPort: 9000