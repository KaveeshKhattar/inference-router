apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference-router
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inference-router
  template:
    metadata:
      labels:
        app: inference-router
    spec:
      containers:
      - name: router
        image: llm-router:token-count-qa-15-apr
        imagePullPolicy: Never
        ports:
        - containerPort: 9000
        env:
        - name: VLLM_DISCOVERY_HOST
          value: "vllm-headless.default.svc.cluster.local"