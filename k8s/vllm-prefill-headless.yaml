apiVersion: v1
kind: Service
metadata:
  name: vllm-prefill-headless
spec:
  clusterIP: None
  selector:
    app: vllm-prefill
    llm-d.ai/role: prefill
  ports:
  - name: http
    port: 8000
    targetPort: 8000
